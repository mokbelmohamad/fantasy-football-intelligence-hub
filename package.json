{
  "name": "fantasy-football-intelligence-hub",
  "version": "2.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test js/tests/*.test.js",
    "update:data": "node scripts/update-data.mjs",
    "validate": "node scripts/validate-project.mjs",
    "check": "npm test && npm run validate"
  },
  "engines": {
    "node": ">=22"
  }
}
