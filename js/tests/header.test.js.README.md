# header.test.js

Version 2.2 structural regression tests for the shared application header.

The tests confirm that the grouped page selector, league context, Focus Team selector, and overflow menu are present; the legacy `topBanner` and `tabs` elements are absent; and the runtime/package version is consistently set to 2.2.0.
